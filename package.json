{
  "name": "buildco-projects",
  "version": "1.0.0",
  "description": "Build Co Projects Website",
  "private": true,
  "scripts": {
    "start": "npx serve",
    "dev": "npx serve",
    "build": "echo 'Static site - no build step required'"
  },
  "dependencies": {
    "stripe": "^12.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
